<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="TerminalAccentBrush" Color="#FF4176F5"/>
    <SolidColorBrush x:Key="TerminalAccentBrushHover" Color="#FF5F89F7"/>
    <SolidColorBrush x:Key="TerminalButtonBackground" Color="#FF2E2E2E"/>
    <SolidColorBrush x:Key="TerminalButtonBackgroundHover" Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="TerminalStatusRunning" Color="#FF38C172"/>
    <SolidColorBrush x:Key="TerminalStatusStopped" Color="#FFE3342F"/>

    <Style x:Key="TerminalButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="{StaticResource TerminalButtonBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TerminalAccentBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource TerminalButtonBackgroundHover}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource TerminalAccentBrushHover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FF1F1F1F"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TerminalIconButtonStyle" BasedOn="{StaticResource TerminalButtonStyle}" TargetType="Button">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Width" Value="26"/>
        <Setter Property="Height" Value="26"/>
    </Style>
</ResourceDictionary>
