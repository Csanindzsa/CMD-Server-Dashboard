<Window x:Class="CmdDashboard.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:CmdDashboard.Controls"
    xmlns:vm="clr-namespace:CmdDashboard.ViewModels"
    xmlns:panels="clr-namespace:CmdDashboard.Panels"
    xmlns:converters="clr-namespace:CmdDashboard.Converters"
        Title="Command Dashboard"
        Height="720"
        Width="1200"
        Background="#FF101010"
    FontFamily="Segoe UI"
    PreviewKeyDown="Window_OnPreviewKeyDown">
    <Window.InputBindings>
        <KeyBinding Modifiers="Control"
                     Key="S"
                     Command="{Binding SaveSelectedNoteCommand}"/>
    </Window.InputBindings>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:ReferenceEqualsConverter x:Key="ReferenceEqualsConverter"/>
        <Style x:Key="DarkContextMenuStyle" TargetType="ContextMenu">
            <Setter Property="Background" Value="#FF1A1A1A"/>
            <Setter Property="BorderBrush" Value="#FF3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <StackPanel IsItemsHost="True"
                                        Background="Transparent"
                                        Margin="0 4"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DarkMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="BackgroundBorder"
                                Background="Transparent"
                                Padding="12 6">
                            <ContentPresenter HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              ContentSource="Header"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#FF2A2A2A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FF555555"/>
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="Transparent"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Separator" x:Key="DarkSeparatorStyle">
            <Setter Property="Margin" Value="0 6"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                                                                        <Grid Background="Transparent"
                                                                                    HorizontalAlignment="Stretch">
                                                                                <Rectangle Fill="White"
                                                                                                     Height="1"
                                                                                                     HorizontalAlignment="Stretch"
                                                                                                     VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NoteTileContainerStyle" TargetType="Border">
            <Setter Property="Background" Value="#FF1A1A1A"/>
            <Setter Property="BorderBrush" Value="#FF3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0 0 12 12"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#FF5F5F5F"/>
                </Trigger>
                <DataTrigger Value="True">
                    <DataTrigger.Binding>
                        <MultiBinding Converter="{StaticResource ReferenceEqualsConverter}">
                            <Binding RelativeSource="{RelativeSource AncestorType=Window}" Path="DataContext.SelectedNote"/>
                            <Binding/>
                        </MultiBinding>
                    </DataTrigger.Binding>
                    <Setter Property="BorderBrush" Value="#FF7F7F7F"/>
                    <Setter Property="Background" Value="#FF2A2A2A"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0 0 0 12">
            <TextBlock Text="Command Dashboard"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Right"/>

            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center"
                        DockPanel.Dock="Left"
                        Margin="0 0 12 0">
                <Button Content="+"
                        FontSize="20"
                        Width="44"
                        Height="36"
                        Margin="0 0 12 0"
                        Click="NewTerminalButton_OnClick"
                        Style="{StaticResource TerminalButtonStyle}"
                        ToolTip="New Terminal"/>
                <Expander x:Name="NotesExpander"
                          Header="Notes"
                          Margin="0 0 12 0"
                          Foreground="White"
                          Background="Transparent"
                          Width="280">
                    <Border Background="#FF1A1A1A"
                            BorderBrush="#FF3A3A3A"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="10">
                        <StackPanel>
                            <TextBlock Text="Hidden notes"
                                       Foreground="#FFAAAAAA"
                                       FontSize="12"
                                       Margin="0 0 0 4"
                                       Visibility="{Binding HasOverflow, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <TextBlock Text="Right-click a note to open actions. Press Delete to remove the selected note and Ctrl+S to save changes."
                                       TextWrapping="Wrap"
                                       Foreground="#FF8A8A8A"
                                       FontSize="11"
                                       Margin="0 0 0 6"/>
                            <ComboBox ItemsSource="{Binding OverflowNotes}"
                                      SelectedItem="{Binding SelectedOverflowNote, Mode=TwoWay}"
                                      DisplayMemberPath="Title"
                                      Background="#FF121212"
                                      Foreground="White"
                                      BorderBrush="#FF3F3F3F"
                                      Visibility="{Binding HasOverflow, Converter={StaticResource BoolToVisibilityConverter}}"
                                      IsEditable="True"
                                      TextSearch.TextPath="Title">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="Background" Value="#FF1E1E1E"/>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                                <ComboBox.Resources>
                                    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF121212"/>
                                    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF121212"/>
                                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF2F6FED"/>
                                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="Background" Value="#FF121212"/>
                                        <Setter Property="BorderBrush" Value="#FF3F3F3F"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Padding" Value="2"/>
                                    </Style>
                                </ComboBox.Resources>
                            </ComboBox>
                        </StackPanel>
                    </Border>
                </Expander>
            </StackPanel>

            <ItemsControl x:Name="NotesTiles"
                          ItemsSource="{Binding VisibleNotes}"
                          Margin="0 0 12 0"
                          VerticalAlignment="Center"
                          Visibility="{Binding IsExpanded, ElementName=NotesExpander, Converter={StaticResource BoolToVisibilityConverter}}"
                          SizeChanged="NotesTiles_OnSizeChanged">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:NoteViewModel}">
                        <Border Style="{StaticResource NoteTileContainerStyle}"
                                MouseLeftButtonDown="NoteTile_OnMouseLeftButtonDown"
                                PreviewMouseRightButtonDown="NoteTile_OnPreviewMouseRightButtonDown"
                                ContextMenuOpening="NoteTile_OnContextMenuOpening">
                            <Border.ContextMenu>
                                <ContextMenu Style="{StaticResource DarkContextMenuStyle}">
                                    <ContextMenu.Resources>
                                        <Style TargetType="MenuItem" BasedOn="{StaticResource DarkMenuItemStyle}"/>
                                        <Style TargetType="Separator" BasedOn="{StaticResource DarkSeparatorStyle}"/>
                                    </ContextMenu.Resources>
                                    <MenuItem Header="New Note"
                                              Command="{Binding AddNoteCommand}"/>
                                    <MenuItem Header="Save"
                                              Command="{Binding SaveSelectedNoteCommand}"/>
                                    <MenuItem Header="Clear"
                                              Command="{Binding ClearSelectedNoteCommand}"/>
                                    <Separator/>
                                    <MenuItem Header="Delete"
                                              Command="{Binding DeleteSelectedNoteCommand}"/>
                                </ContextMenu>
                            </Border.ContextMenu>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <StackPanel Orientation="Horizontal" Grid.Row="0">
                                    <TextBlock Text="{Binding Title}"
                                               FontWeight="SemiBold"
                                               Foreground="White"/>
                                    <Ellipse Width="8"
                                             Height="8"
                                             Margin="8 2 0 0"
                                             VerticalAlignment="Center"
                                             Fill="#FFE0A800"
                                             Visibility="{Binding HasChanges, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </StackPanel>
                                <TextBox Grid.Row="1"
                                         Margin="0 8 0 0"
                                         Text="{Binding Content, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         Height="110"
                                         Background="#FF121212"
                                         Foreground="White"
                                         BorderBrush="#FF3F3F3F"
                                         BorderThickness="1"
                                         FontFamily="Consolas"
                                         FontSize="13"
                                         VerticalScrollBarVisibility="Auto"
                                         GotFocus="NoteTileTextBox_OnGotFocus"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DockPanel>

        <ListBox x:Name="TerminalList"
                 Grid.Row="1"
                 ItemsSource="{Binding Sessions}"
                 Background="Transparent"
                 BorderThickness="0"
                 HorizontalContentAlignment="Stretch"
                 VerticalContentAlignment="Stretch"
                 AllowDrop="True"
                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 PreviewMouseLeftButtonDown="TerminalList_OnPreviewMouseLeftButtonDown"
                 PreviewMouseMove="TerminalList_OnPreviewMouseMove"
                 Drop="TerminalList_OnDrop">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <panels:AdaptiveUniformGrid/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:TerminalSessionViewModel}">
                    <Grid Margin="4"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch">
                        <controls:TerminalTile HorizontalAlignment="Stretch"
                                                VerticalAlignment="Stretch"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
